#  HARDHAT PROJECT FOR IMPLEMENTATION ON ERC721
1. Run `npm install`
2. Install the necessary dependencies
3. Deploy the implementation address > `npx hardhat run scripts/deploy.js`
4. Deploy the Factory address and `npx hardhat run scripts/deploymaster.js`
5. Run a test script / Test case on the clone function
   
